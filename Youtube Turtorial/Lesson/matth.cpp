#include <iostream>
#include <cmath>
using namespace std;

int main (){
    double x =3.39;
    double y =4;
    double z;
    //z = min(x,y);
    //z = max(x,y);
    //z = pow(2,4);
    //z = sqrt(16);
    //z = abs(-3);

    //my dumbass forget to record so this video is going to be 20sec long
    //also today we are learning about useful math function on c++

    //goodbye
    //z = round(x);
    //z = ceil(x);
    z = floor(x);

    cout << z << endl;



    return 0;
}